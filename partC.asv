%% 
n = 0:99;
w = 0.2*pi;
w1 = 0.4*pi;
w2 = 0.1*pi;
x = sin(w.*n) + sin(w1.*pi);

out = multibank(x, 1, 1, 1, 1);

out1 = davis(x);

figure
stem(x)
hold on
stem(out)
legend('Input, x[n]', 'Output, y[n]')
xlabel('n')
title('Output vs Input of Multirate Filter Bank')

%% 

speech_out = multibank(nspeec2', 3, 3, 
